apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kan-brewer.fullname" . }}-test-connection"
  labels:
    {{- include "kan-brewer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kan-brewer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
