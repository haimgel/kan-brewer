apiVersion: v1
kind: Service
metadata:
  name: {{ include "kan-brewer.fullname" . }}
  labels:
    {{- include "kan-brewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kan-brewer.selectorLabels" . | nindent 4 }}
